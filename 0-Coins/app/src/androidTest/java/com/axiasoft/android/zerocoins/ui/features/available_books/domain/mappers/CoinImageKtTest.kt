
package com.axiasoft.android.zerocoins.ui.features.available_books.domain.mappers

/*
class CoinImageKtTest {

    lateinit var nonExistentKeyAsk: Ask
    @Before
    fun setUp() {
        nonExistentKeyAsk = Ask(
            book = "mxn_amlocoin",
            price = "0.00",
            amount = "0.0"
        )
    }

    @After
    fun tearDown() {
    }

    @Test
    fun testThatTestIsWorking(){
        assertThat(true).isTrue()
    }

    @Test
    fun nonValidOrNonExistentKey_isAnyType(){
        val typeOrderBook = getBookOrderType(nonExistentKeyAsk.book ?: "")
        log("z0", typeOrderBook?.coinImage.toString())
        assertThat(CoinNameAndImage.any_any.coinImage == typeOrderBook?.coinImage).isTrue()
    }
}*/
