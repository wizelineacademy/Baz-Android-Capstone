package com.example.cryptocurrency_challenge.repository.model

import com.example.cryptocurrency_challenge.data.model.InfoTickerResponse
import com.example.cryptocurrency_challenge.data.model.Payload_Ticker
import com.example.cryptocurrency_challenge.room.TickerEntity

/*

data class ModelTicker(

    val ticker: Payload_Ticker = TickerEntity(book = "", high = "", last = "", low = ""),
    //val payloadRoom: TickerEntity = TickerEntity(book = "", high = "", last = "", low = ""),
    val success: Boolean = false,

    )

fun Payload_Ticker.toDomain()= ModelTicker(ticker = , success = success)
//fun TickerEntityResponse.toDomain ()= ModelTicker(payload = Payload_Ticker(book = "", high = "", last = "", low = ""), success = success)

*/